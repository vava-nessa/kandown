# Architecture review — 2026-07-27 · Task transition policy

Material generated by the **improve-codebase-architecture** skill run on the
Kandown worktree.

## Contents

| Path | What it is |
|---|---|
| `index.html` | The full visual report (Mermaid + Tailwind). Open it in a browser; it renders without any local server. |
| `assets/related-runs.html` | A second run captured the same day (later timestamp). Kept for reference; not authored as a deliverable. |
| `assets/dependencies-proposal.ts` | The proposed deep `src/lib/dependencies.ts` API (`resolveTransition`, `assertTransitionAllowed`, `isArchivedStatus`, …) that the report's #1 candidate calls for. Not committed to `src/` yet — used as a proto. |
| `assets/dependencies.spec.ts` | The Vitest matrix that locks the policy down. Matches the proposed module; dropped into `src/lib/__tests__/` once the module lands. |

## Top recommendation

Deepen the **Task transition policy** first — repair the shared dependency gate
so the web store, TUI, CLI, MCP, and cascade paths agree on the rule. See
card #1 in `index.html`.

## How to re-run the skill

```bash
# in a future session, at the project root
kandown work
# then invoke the skill
/improve-codebase-architecture
```

Each new review gets its own date-stamped folder so reports stay discoverable.
