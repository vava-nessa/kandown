{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the desktop window. Slice 1 only needed the core defaults; slice 2 adds the dialog plugin permission so `cmd_pick_project` can open the native folder picker.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}