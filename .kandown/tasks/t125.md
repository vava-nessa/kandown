---
id: t125
status: Done
order: 4
created: 2026-04-28
title: Si toutes les taches d'un # ou [] regroupant des taches sous un théme (je ne connais pas le nom d'ailleurs?) et que c'est dans "done" il faut que ce soit barré aussi.
---

## Notes

**Implemented:** Tags (bracketed `[]` or `#hashtag`) in task titles are now crossed out on card badges when ALL tasks with that same tag across all columns are Done.

**Files changed:**
- `src/components/Column.tsx` — computes `doneTags` (all tags where every task is Done), passes to CardStack
- `src/components/CardStack.tsx` — passes `doneTags` to Card
- `src/components/Card.tsx` — accepts `doneTags` prop, strikes through tag in badge if in `doneTags`

**Build:** ✅ passing
