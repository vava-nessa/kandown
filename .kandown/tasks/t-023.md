---
id: t-023
title: "[CLI KB-5] Build, Test & Polish — Vérification end-to-end"
status: Done
report: |
  ## Build
  - `pnpm build:cli` → ⚡️ Build success in 11ms, 40.17 KB
  - `tsc --noEmit` → zero type errors
  ## Verified
  - All 5 new source files compile cleanly with tsup
  - TypeScript strict mode, no any types
  - Regexes hoisted, no inline component definitions
created: 2026-04-18
---

Vérifier que tout compile, tester le flow complet, et polir l'UX.

## Context

Après implémentation des tasks t-019 à t-022, on vérifie que tsup build correctement, que le flow est fluide, et que les edge cases sont gérés.

## Subtasks

- [ ] Vérifier que `pnpm build:cli` (tsup) compile sans erreur avec les nouveaux fichiers
- [ ] Test : `kandown` ouvre le navigateur + TUI board
- [ ] Test : navigation colonnes/tasks au clavier
- [ ] Test : Enter sur une task → vue détail → Esc retour
- [ ] Test : `a` → agent picker → sélection → task passe en In Progress + agent se lance
- [ ] Test : tmux split-pane fonctionne si dans tmux
- [ ] Test : sans tmux, le TUI quitte proprement et l'agent prend le terminal
- [ ] Vérifier que le prompt injecté (AGENT_KANDOWN_COMPACT.md + task content) est correct
