---
id: t122
title: Quand on crée une nouvelle tache, on devrais ouvir la popup directement et mettre le focus dans le titre (vide) pour taper le titre. Et la description devrais etre vide par défaut.
status: Done
order: 4
created: 2026-04-22
report: |
  ## Changes
  - Updated `createTask` in `src/lib/store.ts` to set title and description as empty strings by default.
  - Added `await get().openDrawer(id);` to automatically open the task drawer natively upon creation, which inherently triggers the focus on the title input.
  - Updated `CHANGELOG.md` to document the change block.
---

# New task

## Context


## Subtasks
