---
id: t134
title: Fix task move sync across Web and TUI
status: Done
order: 134
report: |
  ## Changes
  - Fixed web server-mode task moves so drag/drop persists to `tasks/*.md` through the existing REST-backed filesystem adapter.
  - Extended reorder persistence to work with `FileSystemDirectoryHandle | null`, letting browser server mode and local File System Access mode share the same path.
  ## Verification
  - Reproduced the original bug in `/tmp/kandown-repro-1780591052`: web drag moved `t3` optimistically, but the file stayed `status: Todo` and the UI reverted.
  - Verified fixed web drag in `/tmp/kandown-repro-fixed-1780591226`: Chrome DevTools drag moved `t3` to In Progress, file/API wrote `status: In Progress`, and tmux TUI synced.
  - Verified TUI move back to Backlog via tmux keyboard flow; Chrome web UI synced after polling.
  - Ran `CI=true pnpm typecheck` and `CI=true pnpm build` successfully.
  ## Files
  - `src/lib/store.ts` (modified)
  - `src/lib/version.ts` (auto-generated by build)

priority: P1
tags: [bug, sync, cli, web]
assignee: pi
created: 2026-06-04
ownerType: ai
---

# Fix task move sync across Web and TUI

## Context

User reports that moving a task fails or does not synchronize between the web UI and the TUI CLI. Reproduce as a real user in an empty folder, using tmux for terminal flows and Chrome DevTools for the browser flow.

## Subtasks

- [x] Reproduce in a fresh user folder with local Kandown build
  report: Created `/tmp/kandown-repro-1780591052`, initialized Kandown, and launched the real CLI in tmux on port 2058.
- [x] Test Web drag/drop or equivalent move flow with Chrome DevTools
  report: Dragging `t3` from Todo to In Progress updates the web UI optimistically, then server polling reloads it back to Todo because the task file stays `status: Todo`.
- [x] Test TUI move flow inside tmux
  report: TUI keyboard move works when sent like a real user (`m`, `j`, `Enter`, `Enter`) and writes `status: Todo` to `t3.md`; the sync bug is isolated to web server-mode persistence.
- [x] Identify root cause and patch the smallest shared sync/move layer
  report: Root cause was `moveTask`/`reorderInColumn` returning early in browser server mode, so the optimistic web state never wrote through the REST API. Patched shared column persistence to accept `null` handles and use the existing server-mode filesystem adapter.
- [x] Re-run Web + TUI verification
  report: Web drag now persists `status: In Progress` to disk/API and the tmux TUI updates; TUI move back to Backlog also updates disk and the web UI.
- [x] Run build/typecheck and summarize results
  report: `CI=true pnpm typecheck` passed; `CI=true pnpm build` passed and regenerated `src/lib/version.ts`/dist output.
