---
id: t-022
title: "[CLI KB-4] CLI Command Wiring — kandown ouvre TUI + navigateur"
status: Done
report: |
  ## Modified
  - `bin/kandown.js` — bare `kandown` command opens browser + TUI board, new `board` command, cmdOpen(), findKandownDir(), updated help(), copies AGENT_KANDOWN_COMPACT.md on init
  - `src/cli/lib/config.ts` — agents?: { preferred?, extraArgs? } section added to KandownConfig, merged in loadConfig
created: 2026-04-18
---

Câbler les nouvelles commandes dans le CLI : `kandown` (bare) ouvre le TUI board + la page web dans le navigateur. `kandown board` ouvre le TUI seul.

## Context

Actuellement `kandown` sans argument affiche le help. Le nouveau comportement : ouvrir les deux interfaces simultanément (TUI board dans le terminal + kandown.html dans le navigateur).

## Subtasks

- [ ] Modifier `bin/kandown.js` — `kandown` (no args) → ouvre browser (`open`/`xdg-open` le .kandown/kandown.html) + lance TUI board
- [ ] Ajouter commande `kandown board` → lance TUI board uniquement
- [ ] Mettre à jour la fonction `help()` avec les nouvelles commandes
- [ ] Étendre `src/cli/lib/config.ts` — ajouter section `agents` optionnelle au type KandownConfig (preferred agent, extraArgs par agent)

## Notes

- Ouvrir le navigateur : `open` sur macOS, `xdg-open` sur Linux, `start` sur Windows
- Le browser s'ouvre de façon non-bloquante (spawn detached) avant de lancer le TUI
- L'ancien `kandown help` reste accessible
