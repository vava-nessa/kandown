# 📖 Publishes kandown to npm when a version tag (v*) is pushed.
# Triggered by: `git tag v0.1.0 && git push origin v0.1.0`
# or by the "bump" workflow described in AGENTS.md.

name: Publish to npm

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          registry-url: "https://registry.npmjs.org"
          cache: pnpm

      - run: pnpm install --no-frozen-lockfile

      - run: pnpm build

      - run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # 📖 Skip if release already exists (created by bump workflow)
          if gh release view "$GITHUB_REF_NAME" &>/dev/null; then
            echo "Release $GITHUB_REF_NAME already exists, skipping."
            exit 0
          fi
          VERSION="${GITHUB_REF_NAME#v}"
          # 📖 One file per release (changelogs/vX.Y.Z.md) — read it directly
          # instead of slicing a range out of the index. `tail -n +2` drops the
          # `# 0.36.1 — … — "Name"` heading, which the release title repeats.
          NOTES=$(tail -n +2 "changelogs/v${VERSION}.md")
          gh release create "$GITHUB_REF_NAME" \
            --title "v${VERSION}" \
            --notes "$NOTES"
