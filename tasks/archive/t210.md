---
id: t210
title: Add kandown work output configurator
status: Done
order: 0
priority: P1
tags: [settings, cli, agent]
created: 2026-07-20
ownerType: ai
report: |
  ## Changes
  - Added `agent.workOutput` project config for block/raw `kandown work` rendering.
  - Added concise token-efficient Kandown rules mode.
  - Added an estimated token counter and output preview in Settings.
  - Added Settings controls for section toggles, board digest details, raw template editing, and `.kandown/instructions.md` editing.
  - Added authenticated server endpoints and browser helpers for `.kandown/instructions.md`.
  - Added `.kandown/AGENT_KANDOWN.md` sync/heal behavior so missing, malformed, or stale generated docs are recreated inside `.kandown/` only.
  - Updated README documentation for the new work-output configurator and token-efficient mode.
  ## Decisions
  - Kept user-owned custom prose in `.kandown/instructions.md`, not in generated `AGENT_KANDOWN.md`.
  - Kept storage project-scoped in `.kandown/kandown.json` as requested.
  - Implemented raw templates with explicit `{{baseRules}}`, `{{projectInstructions}}`, and `{{boardDigest}}` variables.
  ## Validation
  - `pnpm typecheck` passed.
  - `pnpm build` passed.
  - `node bin/kandown.js work` passed and recreated `.kandown/AGENT_KANDOWN.md`.
  - Temp-project smoke tests passed for concise blocks mode and raw template mode.
  - `node bin/kandown.js doctor` passed.
  ## Files
  - README.md
  - bin/kandown.js
  - src/components/SettingsPage.tsx
  - src/lib/filesystem.ts
  - src/lib/types.ts
  - templates/kandown.json
  - tasks/t210.md
archived: true
---

# Add kandown work output configurator

## Context

Vava wants Settings to control the markdown emitted by `kandown work`, with block-level options plus an advanced raw mode. Generated agent docs must stay inside `.kandown/`: recreate `.kandown/AGENT_KANDOWN.md` when missing and overwrite malformed/stale generated copies instead of polluting project-root agent files.

## Subtasks

- [x] Add project config schema for `agent.workOutput`
  report: Added typed work-output config with block/raw mode, section toggles, digest toggles, raw template, and concise/full base-rules mode.
- [x] Add concise/token-efficient work output mode and token estimate counter
  report: Added concise Kandown rules in CLI rendering and a Settings-side approximate token counter for the configured output.
- [x] Make `kandown work` render from block options or raw template
  report: Updated `cmdWork` to read project config, render ordered blocks, or apply raw templates with baseRules/projectInstructions/boardDigest variables.
- [x] Add safe `.kandown/AGENT_KANDOWN.md` sync/heal logic
  report: Added sync/heal logic that recreates missing docs and overwrites malformed/stale generated `.kandown/AGENT_KANDOWN.md` from the package template.
- [x] Add Settings UI to edit project instructions and work output
  report: Added the Agent settings configurator with block/raw controls, digest detail toggles, token-efficient mode, project instructions editor, and preview.
- [x] Add server/browser filesystem helpers for `.kandown/instructions.md`
  report: Added browser File System Access helpers plus authenticated server REST endpoints for reading/writing `.kandown/instructions.md`.
- [x] Validate with typecheck/build and CLI smoke tests
  report: Ran pnpm typecheck, pnpm build, normal work output, temp concise work output, temp raw work output, and doctor successfully.
- [x] Completion report
  report: Added frontmatter completion report with changes, decisions, validation, and affected files.
