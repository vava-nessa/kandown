---
id: t211
status: Done
order: 5
report: |
  ## Changes
  - Added task deep-link parsing/building in `src/lib/task-url.ts`.
  - Synced drawer open/close with shareable URLs like `/210?p=kandown`.
  - Added direct link hydration so pasted URLs open the matching drawer after project load.
  - Added a drawer "Copy URL" action.
  - Updated the daemon to serve task deep-link paths instead of returning 404.
  - Follow-up: restarting `kandown` now replaces stale pre-deep-link daemons, because old daemon processes cannot learn new `/058?p=project` routing just by refreshing `kandown.html`.
  ## Validation
  - `pnpm typecheck` passes.
  - `node --check bin/kandown.js` passes.
  ## Files
  - `src/lib/task-url.ts` (new)
  - `src/App.tsx`
  - `src/lib/store.ts`
  - `src/components/Drawer.tsx`
  - `src/components/Header.tsx`
  - `bin/kandown.js`
  - `tasks/t211.md`
created: 2026-07-20
title: URL task based
archived: true
---

j'aimerais qu'on implémente la possibilité de pouvoir copier-coller l'URL d'une tâche. par ex <http://localhost:2050/?p=kandown>/210 pour la tache 210.

## Subtasks

- [x] Add task URL parser/builders
  report: Created `src/lib/task-url.ts` to normalize `/210`, `/t210`, `?task=210`, and `?p=kandown/210` into canonical task ids.
- [x] Sync drawer state with the browser URL
  report: Opening a drawer now writes `/210?p=kandown`; direct links and browser back/forward open or close the drawer.
- [x] Serve task deep-link paths from the daemon
  report: The local daemon now serves the SPA for single-segment task paths like `/210`, so pasted URLs don't 404 before React loads.
- [x] Restart stale daemons after CLI updates
  report: `startDaemon` now compares daemon metadata version with the current CLI version and restarts older daemons, fixing refresh 404s caused by long-running pre-deep-link daemon code.
