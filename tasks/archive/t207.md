---
id: t207
title: Add sectioned drag-and-drop list view
status: Done
order: 0
priority: P2
tags: [web-ui, list-view, drag-drop]
created: 2026-07-20
ownerType: ai
report: |
  ## Changes
  - Rebuilt ListView as vertical sections matching board columns.
  - Added vertical drag-and-drop section reordering persisted through reorderColumns.
  - Added task row drag-and-drop between list sections persisted through moveTask.
  ## Decisions
  - Reused existing store actions instead of adding a second persistence path.
  - Kept task row layout dense so List view stays list-first, not a copied board card view.
  ## Validation
  - pnpm typecheck passes.
  - pnpm build passes.
  ## Files
  - src/components/ListView.tsx (modified)
  - tasks/t207.md (work log)
archived: true
---

# Add sectioned drag-and-drop list view

## Context

Vava wants the List view to mirror the Kanban board in the opposite direction: board columns become horizontal list sections stacked vertically, and those sections can be reordered with vertical drag-and-drop.

## Subtasks

- [x] Inspect current Board/List drag behavior
  [REPORT] Reviewed Board.tsx/Column.tsx drag handlers, ListView.tsx table rendering, and store moveTask/reorderColumns APIs.
- [x] Group List view by board columns as horizontal sections
  [REPORT] Replaced the flat table with column-backed horizontal sections stacked vertically.
- [x] Add vertical section reordering in List view
  [REPORT] Added section gripper, vertical drop-slot calculation, and persisted reorderColumns integration.
- [x] Add task drag/drop between list sections
  [REPORT] Added draggable task rows and section drop targets wired to moveTask for status changes.
- [x] Run typecheck validation
  [REPORT] Ran pnpm typecheck and fixed the Motion drag event typing issue by using capture-phase native drag handlers.
- [x] Run build validation
  [REPORT] Ran pnpm build successfully, including version injection, synced AGENT_KANDOWN, Vite production bundle, and CLI tsup build.
- [x] Final completion report
  [REPORT] Added the frontmatter completion report with changes, decisions, validation, and affected files.
