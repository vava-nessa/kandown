---
id: t221
title: Audit and restore CLI commands broken by modular split
status: Done
order: 0
priority: P1
report: |
  ## Changes
  - Restored the modular CLI command surface after the split regression: task commands, TUI commands, daemon subcommands, MCP dispatch, export/import/projects commands, and executable TUI entrypoint.
  - Restored script-safe stdout behavior by sending decorative status/init messages to stderr instead of polluting JSON, MCP, or command-substitution output.
  - Hardened the auto-updater to update the Node/npm prefix that owns the active `kandown` binary, instead of accidentally installing into another Node prefix on machines with multiple npm installs.
  - Rebuilt and reinstalled the fixed package into the active nvm global prefix.
  ## Verification
  - `pnpm build` passes.
  - Fresh-project auto-init works through `kandown create` and `kandown --no-open`.
  - `kandown create/show/list/move/assign/tasks` smoke tests pass.
  - `kandown daemon start/status/restart/stop` pass.
  - `kandown board` and `kandown settings` dispatch safely in non-interactive shells and will launch the TUI in a real terminal.
  - `kandown mcp` keeps stdout JSON-only while init notices go to stderr.
  - `kandown import <json>` imports list/export-shaped JSON into task files.
  - `kandown doctor` passes and `npm pack --dry-run` includes rebuilt bundles.
  ## Files
  - `src/cli/cli.ts` — restored command dispatch and task/daemon/TUI command handlers.
  - `src/cli/lib/updater.ts` — made global updates prefer the npm/pnpm binary next to the active `kandown` executable.
  - `src/cli/tui.tsx` — added direct executable entrypoint for `bin/tui.js`.
  - `src/cli/lib/init.ts` — initializer module used by auto-init and `init`.
  - `src/cli/lib/server.ts` — package-root HTML sync and no-op symlink mutation from the previous repair.
  - `bin/kandown.js`, `bin/tui.js` — rebuilt CLI bundles.
  - `src/lib/version.ts`, `.kandown/kandown.html` — regenerated build artifacts.
  - `tasks/t221.md` — work log and completion report.
tags: [cli, regression]
assignee: Pi
created: 2026-07-21
ownerType: ai
archived: true
---

# Audit and restore CLI commands broken by modular split

## Context

After the CLI split into smaller files, several commands advertised in the README/help regressed or disappeared. Vava asked to fix all of it.

## Subtasks

- [x] Compare current CLI command surface against README and older working CLI
  report: Compared the modular CLI against the README and v0.18.0 monolithic CLI. Missing/broken surfaces included create/new, assign, commit, tasks help, board/settings TUI entrypoints, daemon start/restart/refresh-all, mcp, and direct TUI execution.
- [x] Restore missing one-shot task commands
  report: Restored `list` filters/JSON, raw `show`, `create/new`, `move archived`, `assign`, `commit`, and `tasks` help in the modular CLI.
- [x] Restore TUI-only commands and daemon subcommands
  report: Added `board` and `settings` dispatch, explicit `bin/tui.js` direct execution, and daemon `start`, `restart`, `refresh-all`, `status`, `stop`, and `run` handling.
- [x] Verify auto-init and command contracts in fresh projects
  report: Fresh `/tmp` projects now auto-init from `kandown create` and default launch. Verified stdout contract for command substitution and JSON output.
- [x] Rebuild, reinstall, and run final checks
  report: Ran `pnpm build`, reinstalled into `/Users/vava/.nvm/versions/node/v25.2.1`, verified daemon lifecycle, MCP stdout, doctor, and pack dry-run.
- [x] Write completion report
  report: Added the final frontmatter report with the restored command surface and verification list.
