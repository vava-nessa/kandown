---
id: t220
title: Restore kandown auto-init when launched in a fresh project
status: Done
order: 0
priority: P1
report: |
  ## Changes
  - Restored the previous zero-setup behavior: running `kandown` in a fresh project now creates `.kandown/`, copies templates, creates `tasks/`, starts the web daemon, and launches through the normal app path.
  - Rebuilt and reinstalled the fixed CLI into the active nvm global prefix.
  ## Verification
  - `pnpm build` passes.
  - In a fresh `/tmp` folder, `kandown --no-open` auto-created `.kandown/` and `tasks/`, then started a daemon.
  - Existing project launch still works.
  - `kandown doctor` passes.
  - `npm pack --dry-run` includes the rebuilt CLI bundle.
  ## Files
  - `src/cli/cli.ts` — restored auto-init in the shared Kandown directory resolver.
  - `bin/kandown.js` — rebuilt CLI bundle.
  - `src/lib/version.ts` — regenerated build timestamp.
  - `.kandown/kandown.html` — refreshed local web bundle.
  - `tasks/t220.md` — work log and completion report.
tags: [cli, init]
assignee: Pi
created: 2026-07-21
ownerType: ai
archived: true
---

# Restore kandown auto-init when launched in a fresh project

## Context

Vava confirmed that before the CLI split, running `kandown` from a project without `.kandown/` automatically initialized the board and launched it.

## Subtasks

- [x] Restore auto-init for default `kandown` launch
  report: Changed the shared CLI `ensureKandownDir` helper to call `doInit()` automatically when `.kandown/` is missing, restoring the old zero-setup startup behavior.
- [x] Check scripted task commands for the same regression
  report: The same helper is used by list/show/move/work/doctor/default launch and daemon paths, so those commands now regain auto-init behavior too.
- [x] Rebuild and reinstall the fixed global CLI
  report: Ran `pnpm build`, then installed the rebuilt package into `/Users/vava/.nvm/versions/node/v25.2.1` so the active `kandown` command uses the fix.
- [x] Verify in a fresh temporary project
  report: In `/tmp/kandown-autoinit.*`, `kandown --no-open` created `.kandown/`, copied templates, created `tasks/t1.md`, started the daemon, and `kandown daemon stop` stopped it cleanly.
- [x] Write completion report
  report: Added the final frontmatter report with changes, verification, and affected files.
