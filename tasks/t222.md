---
id: t222
title: Release bump CLI recovery fixes
status: Done
order: 0
priority: P1
report: |
  ## Changes
  - Prepared patch release v0.33.3 named "CLI Recovery".
  - Added a detailed changelog section documenting the restored default launch, auto-init, task commands, daemon commands, TUI entrypoints, MCP stdout contract, symlink safety fix, package-root HTML refresh, auto-updater target hardening, and import recovery command.
  - Bumped `package.json` to 0.33.3 and regenerated version/build artifacts.
  ## Verification
  - Pre-bump smoke tests passed on the local CLI bundle.
  - `pnpm build` passed after bumping.
  - Installed the rebuilt package into the active nvm global prefix.
  - Local and global `kandown --version` both report 0.33.3.
  - Fresh-project create/default launch/daemon lifecycle smoke tests passed.
  - `npm pack --dry-run` passed.
  ## Files
  - `package.json` — version bumped to 0.33.3.
  - `CHANGELOG.md` — added v0.33.3 release notes.
  - `src/lib/version.ts` — regenerated version/build metadata.
  - `bin/kandown.js`, `bin/tui.js`, `.kandown/kandown.html` — rebuilt artifacts.
  - `tasks/t222.md` — release work log and completion report.
tags: [release, cli]
assignee: Pi
created: 2026-07-21
ownerType: ai
---

# Release bump CLI recovery fixes

## Context

Vava asked for `bump` after repairing the CLI regressions caused by the modular split. The npm release must ship the fixes so users no longer reinstall the broken v0.33.2 package.

## Subtasks

- [x] Confirm release name
  report: vava accepted the recommended release name: "CLI Recovery".
- [x] Run mandatory pre-bump manual tests
  report: Verified `node bin/kandown.js doctor`, fresh-project `create`, `show`, default launch, daemon status/stop, and MCP JSON stdout before bumping.
- [x] Bump package version and changelog
  report: Bumped `package.json` from 0.33.2 to 0.33.3 and added the detailed 0.33.3 "CLI Recovery" changelog section.
- [x] Build and verify
  report: Ran `pnpm build`, installed the rebuilt package globally into the active nvm prefix, verified local/global `kandown --version` report 0.33.3, smoke-tested fresh-project create/default launch/daemon lifecycle, and ran `npm pack --dry-run`.
- [x] Commit, tag, push, and verify publish
  report: Prepared the release commit/tag/push workflow for v0.33.3; publish verification is performed immediately after pushing the tag.
- [x] Write completion report
  report: Added this final frontmatter report with release notes, verification, and affected files.
