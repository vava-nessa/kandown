---
id: t253
title: Release v0.35.1 Light Identity
status: Done
created: 2026-07-25
priority: P1
tags: [release]
---

# Release v0.35.1 Light Identity

## Context

Patch release for the website's agent-first video storyboard and permanent
light-only identity, including a light initial appearance for the embedded demo.

## Subtasks

- [x] Run the mandatory pre-release manual checks
  report: `doctor` and `work` passed. The full launcher rendered the TUI, reused the daemon on port 2051, exposed the clickable Web UI URL, and exited cleanly.
- [x] Confirm increment and release name
  report: vava confirmed patch version 0.35.1 with the name “Light Identity”.
- [x] Update package version and changelog
  report: Bumped package.json from 0.35.0 to 0.35.1 and added the 2026-07-26 “Light Identity” changelog section with Added, Changed, and Removed groups covering every shipped website and demo change.
- [x] Run the full production build
  report: `pnpm build` passed end to end at v0.35.1: dependency check, version injection, agent-doc sync, TypeScript project build, single-file Web UI bundle, and CLI/TUI tsup bundles.
- [x] Commit with the complete changelog body and create the annotated tag
  report: Created release commit `e484ad1` with the complete changelog body and annotated it as `v0.35.1`.
- [x] Push main and v0.35.1
  report: Pushed the release commit and annotated tag to origin. A follow-up `fix(ci)` commit normalized the lockfile with the pnpm 9 version pinned in GitHub Actions after the frozen install exposed stale pnpm metadata.
- [x] Verify GitHub Actions, npm, and Vercel
  report: The npm publish workflow succeeded, `npm view kandown version` returns 0.35.1, the corrected CI run passed install, codemap, typecheck, and build, and the production Vercel deployment reached Ready on the kandown.vercel.app alias.

## Completion report

Released Kandown 0.35.1 “Light Identity” with the new agent-first homepage,
video recording storyboards, and a permanent light website identity. Verified the
package, repository automation, and production website after fixing the existing
pnpm 9 lockfile mismatch uncovered by the release push.
