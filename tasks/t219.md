---
id: t219
title: Repair CLI launch and auto-updater restart
status: Done
order: 0
priority: P1
report: |
  ## Changes
  - Repaired CLI command dispatch so global flags such as `--no-update-check` no longer masquerade as commands.
  - Restored `kandown init` by wiring the initializer module into the CLI and copying the expected `.kandown/AGENT.md` template.
  - Fixed daemon launch parsing, including `daemon run --port`, which is used by the app startup path.
  - Removed the dangerous daemon-side global symlink rewrite that could point `kandown` to a wrong bundle-relative path and brick the command.
  - Fixed project HTML refresh to use the package root instead of a broken `import.meta.url` relative path.
  - Rebuilt the package and installed the fixed local package into the active nvm global prefix.
  ## Verification
  - `pnpm build` passes.
  - `kandown --version` prints `0.33.2`.
  - `kandown --no-open` starts the project daemon successfully.
  - `kandown daemon status` detects the daemon and `kandown daemon stop` stops it.
  - `kandown doctor` passes.
  - `npm pack --dry-run` includes the repaired bundled CLI files.
  ## Files
  - `src/cli/cli.ts` — fixed command parsing and restored init dispatch.
  - `src/cli/lib/init.ts` — added initializer module to the source tree.
  - `src/cli/lib/server.ts` — removed symlink mutation and fixed package-root HTML sync.
  - `bin/kandown.js` — rebuilt CLI bundle.
  - `src/lib/version.ts` — regenerated build timestamp.
  - `tasks/t219.md` — work log and completion report.
tags: [cli, updater]
assignee: Pi
created: 2026-07-21
ownerType: ai
---

# Repair CLI launch and auto-updater restart

## Context

Vava reported that a Gemini change broke the auto-updater and the main `kandown` command that should start the web daemon plus the TUI.

## Subtasks

- [x] Reproduce the CLI launch failure
  report: `node bin/kandown.js --no-open` only printed help because the CLI treated a leading global flag as the command. The same bug breaks daemon spawn because it launches `--no-update-check daemon run`.
- [x] Patch global flag handling and updater restart args
  report: Updated the CLI dispatcher so `--no-update-check` is treated as a global flag, `kandown --no-open` still launches the default app flow, daemon subcommands parse flags safely, and `kandown init` is wired back to the initializer module.
- [x] Verify daemon, TUI launch path, build, and key CLI commands
  report: Verified local and global CLI launch, daemon status/stop, explicit daemon run with a preferred port, `kandown doctor`, full `pnpm build`, and `npm pack --dry-run`.
- [x] Write completion report
  report: Added the final frontmatter report with changes, verification, and affected files.
