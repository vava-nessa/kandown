---
id: t206
title: [fable] Implement FABLE_CLI Partie 1 remaining fixes
status: Done
created: 2026-07-19
priority: P1
tags: [cli]
---

## Report

Implemented FABLE_CLI Partie 1 remaining fixes:

- **M1 update policy**: check skipped for `shell`/`daemon` commands, non-TTY and `KANDOWN_NO_UPDATE=1`; 24h throttle via `.update-check.json`; prerelease-safe `semverGt`.
- **M2 stdout/stderr contract**: `log()` → stderr (decorations), new `out()` → stdout (data only: help, tables, ids, JSON). Verified: `$(kandown shell create ...)` captures the id only; `--json | jq` clean.
- **M5 API token**: per-daemon `X-Kandown-Token` (crypto random, in daemon.json + injected as `window.__KANDOWN_TOKEN__`), required on all routes except `GET /api/daemon`; CORS `*` removed; web `apiFetch` + TUI agent-hook send it. Verified 401/200 live.
- **M6 atomic writes**: temp+rename helper in bin + `src/cli/lib/atomic-write.ts`; applied to task/config/board/daemon-metadata writes.
- **M7 daemon spawn lock**: O_EXCL `daemon.lock` (15s stale steal) around spawn in bin `startDaemon`; gitignored.
- **Hardening**: global uncaught/unhandled handlers (daemon stays alive), 10MB body cap (413), config shape validation on PUT.
- **Dette**: removed dead code (`findKandownDir`, `tuiSpinner`, unused vars, double status validation), `rmdirSync`→`rmSync`, shell help/usage now say `kandown shell <cmd>`.

Remaining (deliberately deferred): TS-side spawn lock in `src/cli/lib/daemon.ts` startProjectDaemon, watcher mtime pre-check, launcher E2BIG fallback, settings languages list, `which`→`where` on Windows.
