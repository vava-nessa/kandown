---
id: t242
title: Sanitize i18n strings passed to dangerouslySetInnerHTML
status: Done
priority: P3
tags: [web, security, i18n]
ownerType: agent
created: 2026-07-25
order: 14
updated: 2026-07-27T15:10:38Z
assignee: minimax-code
report: |
  ## Changes
  - Replaced all 3 `dangerouslySetInnerHTML` call sites in i18n-bound strings
    with `<Trans>` components from react-i18next.
  - `src/components/EmptyState.tsx` (2 sites): `emptyState.unsupportedBrowserDesc`
    and `emptyState.selectFolderDesc` — now rendered via `<Trans i18nKey="...">`
    with the children template providing the `<code>` and `<strong>` structure.
  - `src/components/ConflictModal.tsx` (1 site): `conflict.conflictingChanges` —
    `<Trans i18nKey="..." values={{ taskId: ... }}>` escapes the interpolated
    task id; the children template declares the `<code>{{taskId}}</code>` shape so
    react-i18next can map translated HTML onto it.

  ## Decisions
  - Children-as-template pattern: each call site passes the English children
    containing the literal HTML tags and `{{var}}` placeholders. react-i18next
    maps the matching HTML from the 48 locale JSONs onto those tags, and
    interpolates values with React-escaped text. No sanitizer dependency added.
  - No config changes needed: the default `transKeepBasicHtmlNodesFor` /
    children-driven parsing already covers `<code>`, `<strong>`, `<br>`, `<i>`,
    `<p>` for all 48 locales (verified their HTML structure is preserved in
    ja, ar, de, zh, fr at minimum).
  - `agentIcons.tsx:183` still uses `dangerouslySetInnerHTML` but for AI-agent
    brand SVGs sourced from a curated allow-list, not user-controllable i18n
    strings — explicitly out of scope per the original audit ("project-controlled
    locale files" was the surface in question).

  ## Verification
  - `pnpm typecheck` ✅
  - `pnpm build` ✅ (vite + tsup both green; strings present in dist bundle)

  ## Files
  - src/components/EmptyState.tsx (modified: 2 `dangerouslySetInnerHTML` → `<Trans>`,
    added `Trans` to react-i18next import)
  - src/components/ConflictModal.tsx (modified: 1 `dangerouslySetInnerHTML` → `<Trans>`
    with `values`, added `Trans` to react-i18next import)

  ## Pending
  - Lint rule banning new `dangerouslySetInnerHTML` — blocked on [[t229]]
    (ESLint + Prettier setup) which is still in Backlog.
---

# Sanitize i18n strings passed to dangerouslySetInnerHTML

## Context

Three call sites still inject translated strings as raw HTML:

- `src/components/EmptyState.tsx:38` and `:99`
- `src/components/ConflictModal.tsx:69`

The severity is **low**, not critical: these are project-controlled locale files,
not user or agent content — and the original, genuinely dangerous case flagged in
`AUDIT.md` (rendering compiled task markdown through `dangerouslySetInnerHTML` in
the old `MarkdownEditor.tsx`) is gone, since editing moved to BlockNote.

It still deserves closing. kandown ships 48 locales; a translation contributed
through a PR is a plausible injection path, and interpolated values like
`{{taskId}}` come from the board.

## Subtasks

- [x] Replace the three call sites with `<Trans>` components or explicit JSX so no
      raw HTML is injected at all — preferred over adding a sanitizer dependency
  report: Done — `<Trans>` from react-i18next at all 3 call sites (2 in
  EmptyState.tsx, 1 in ConflictModal.tsx). Children-as-template pattern.
- [x] If any case genuinely needs HTML, escape the interpolated values first
  report: Done — `{{taskId}}` in ConflictModal goes through `<Trans values={{...}}>`
  which React-escapes the interpolated string. No raw HTML injection path left
  for the three sites in scope.
- [ ] Add a lint rule banning new `dangerouslySetInnerHTML` once ESLint lands ([[t229]])
  report: Pending — depends on [[t229]] (Configure ESLint + Prettier) which is
  still in Backlog. Will be picked up there.

## Notes

Source: `AUDIT.md`, application-level vulnerabilities. The path-traversal and `axios`
findings from that same audit are both already resolved.
