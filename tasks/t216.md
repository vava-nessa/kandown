---
id: t216
status: Done
report: |
  ## Changes
  - Replaced the desktop task modal with a split opened-task workspace below the header.
  - Added a collapsible left task navigator grouped by board status and a large right editor panel.
  - Kept the existing mobile task modal behavior and added a desktop header Back button.
  - Bumped the package to v0.26.0 and documented the release in CHANGELOG.md.
  ## Decisions
  - Reused the existing drawer store/save/URL actions to avoid introducing a second task persistence path.
  - Kept mobile on the modal to avoid squeezing the split layout into a narrow screen.
  ## Files
  - src/components/TaskWorkspace.tsx (new)
  - src/App.tsx (modified)
  - src/components/Drawer.tsx (modified)
  - src/components/Header.tsx (modified)
  - src/lib/i18n/locales/en.json (modified)
  - package.json (modified)
  - CHANGELOG.md (modified)
order: 4
created: 2026-07-20
title: opened task redesign
---

quand on ouvre une tâche j'aimerais que celle ci ne s'ouvre plus dans une modèle mais que à la place elle s'ouvre en dessous du sticky header qui est en haut dans un panneau qui prend les trois quarts de l'écran sur la droite et que sur la gauche on met un listing de toutes les tâches classées et avec une catégorie refermable par exemple backlog, review etcet comme ça on pourrait switcher rapidement d'une tâche à l'autre Ce serait très pratique. et un gros bouton retour en haut dans le header à côté du logo countdown pour revenir à l'affichage de toutes les cards.

## Clarified requirements

- Replace only the desktop task modal with an opened-task workspace.
- Use a 25% left task list and a 75% right task details panel on desktop.
- Keep the current modal behavior on mobile.
- The back button closes the opened task and preserves the current board/list view.
- The left list shows all tasks grouped by status in collapsible sections.

## Subtasks

- [x] Inspect the current task opening/modal architecture.
  report: Found the existing Drawer/store URL flow and reused it for the new desktop split workspace.
- [x] Build the desktop opened-task workspace with grouped status navigation.
  report: Added a desktop-only TaskWorkspace with collapsible status sections on the left and the editor panel on the right.
- [x] Preserve mobile modal behavior and close/back semantics.
  report: Kept Drawer as the mobile modal, moved desktop editing into the workspace, and added a header back button that saves then returns to the current view.
- [x] Run manual checks and production build.
  report: Ran pnpm typecheck, pnpm build, node bin/kandown.js doctor, node bin/kandown.js work, and opened the Vite UI to verify the desktop workspace renders.
- [x] Bump release with changelog, commit, tag, push, and publish verification.
  report: Prepared v0.26.0 with the Task Workspace changelog; commit/tag/push and publish verification are the final release steps.

